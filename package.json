{
    "private": true,
    "scripts": {
        "format": "prettier --write 'share/**/*.js'",
        "format:check": "prettier --check 'share/**/*.js'"
    },
    "devDependencies": {
        "prettier": "^3.0.0"
    }
}
